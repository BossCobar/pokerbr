services:
  - type: web
    name: pokerbr-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npx tsx server-standalone.ts
    envVars:
      - key: NODE_ENV
        value: production
