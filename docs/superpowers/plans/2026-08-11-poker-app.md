# Poker Online Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a full-featured Texas Hold'em + Cucurucho online poker app for friends, deployable locally and on Vercel.

**Architecture:** Custom Next.js 14 server (server.ts) integrates Socket.io WebSockets for real-time game state. Pure game logic lives in `src/game/` (no I/O). Server-side room/session management in `src/server/`. React frontend with Zustand store in `src/app/` and `src/components/`.

**Tech Stack:** Next.js 14 (App Router), TypeScript, Socket.io, Tailwind CSS, Framer Motion, Howler.js, pokersolver, Zustand, Vitest

---

## File Map

```
server.ts                          # Custom HTTP+Socket.io server entry
src/
  app/
    layout.tsx                     # Root layout, fonts
    globals.css                    # Dark poker theme CSS vars
    page.tsx                       # Home: create/join room
    room/[code]/page.tsx           # Poker table page
  game/
    types.ts                       # ALL shared types (Card, Player, GameState…)
    deck.ts                        # Deck creation + shuffle + deal
    hand-evaluator.ts              # Wrap pokersolver, compare hands
    holdem.ts                      # Texas Hold'em engine (pure functions)
    cucurucho.ts                   # Cucurucho mode extensions
  server/
    room-manager.ts                # In-memory room registry
    game-session.ts                # One GameSession per room
    socket-handlers.ts             # Socket.io event wiring
  components/
    home/HomeScreen.tsx            # Create/join UI
    cards/Card.tsx                 # SVG playing card
    cards/CardBack.tsx             # Face-down card
    table/PokerTable.tsx           # Table orchestrator
    table/TableFelt.tsx            # Oval felt background
    table/PlayerSeat.tsx           # Seat with avatar+chips+cards
    table/CommunityCards.tsx       # Flop/turn/river
    table/Pot.tsx                  # Pot display
    table/BettingControls.tsx      # Fold/Check/Call/Raise/AllIn
    table/ActionTimer.tsx          # Countdown timer
    table/Chat.tsx                 # In-table chat
    table/HandHistory.tsx          # Last hand result panel
    table/WinnerOverlay.tsx        # Winner announcement animation
    table/SpectatorBar.tsx         # Spectator count + toggle
  lib/
    socket.ts                      # Client Socket.io singleton
    store.ts                       # Zustand game store
    hooks/useGame.ts               # Game state + actions hook
    hooks/useTimer.ts              # Turn timer hook
    sounds.ts                      # Howler.js sound manager
tests/
  game/deck.test.ts
  game/hand-evaluator.test.ts
  game/holdem.test.ts
```

---

## Task 1: Project Initialization

**Files:** `package.json`, `next.config.ts`, `tailwind.config.ts`, `tsconfig.json`, `vitest.config.ts`, `server.ts` (skeleton)

- [ ] Run in `C:\cursor\poker`:

```bash
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --yes
```

- [ ] Install runtime dependencies:

```bash
npm install socket.io socket.io-client pokersolver zustand framer-motion howler
npm install --save-dev tsx vitest @vitest/ui @types/howler
```

- [ ] Replace `package.json` scripts section:

```json
"scripts": {
  "dev": "tsx watch server.ts",
  "build": "next build",
  "start": "NODE_ENV=production tsx server.ts",
  "test": "vitest run",
  "test:ui": "vitest --ui"
}
```

- [ ] Create `vitest.config.ts`:

```ts
import { defineConfig } from 'vitest/config';
import path from 'path';

export default defineConfig({
  test: {
    environment: 'node',
    globals: true,
  },
  resolve: {
    alias: { '@': path.resolve(__dirname, './src') },
  },
});
```

- [ ] Create `server.ts`:

```ts
import { createServer } from 'http';
import next from 'next';
import { Server } from 'socket.io';
import { setupSocketHandlers } from './src/server/socket-handlers';

const dev = process.env.NODE_ENV !== 'production';
const app = next({ dev });
const handler = app.getRequestHandler();

app.prepare().then(() => {
  const httpServer = createServer(handler);
  const io = new Server(httpServer, {
    cors: { origin: '*' },
  });
  setupSocketHandlers(io);
  const port = parseInt(process.env.PORT ?? '3000', 10);
  httpServer.listen(port, () => {
    console.log(`> Ready on http://localhost:${port}`);
  });
});
```

- [ ] Commit: `git init && git add -A && git commit -m "chore: initialize Next.js poker project"`

---

## Task 2: Game Types

**Files:** `src/game/types.ts`

- [ ] Create `src/game/types.ts`:

```ts
export type Suit = 'c' | 'd' | 'h' | 's';
export type Rank = '2'|'3'|'4'|'5'|'6'|'7'|'8'|'9'|'T'|'J'|'Q'|'K'|'A';
export type CardCode = string; // e.g. 'As', 'Kh', '2c'

export type GameMode = 'holdem' | 'cucurucho';

export type PlayerStatus = 'waiting' | 'active' | 'folded' | 'allin' | 'sitting-out' | 'spectating';

export interface Player {
  id: string;
  nickname: string;
  seatIndex: number | null; // null = spectator
  chips: number;
  status: PlayerStatus;
  bet: number;           // current street bet
  totalBet: number;      // total bet this hand
  holeCards: CardCode[]; // empty until dealt (server only sends own cards to client)
  isDealer: boolean;
  isSB: boolean;
  isBB: boolean;
  stats: PlayerStats;
  isConnected: boolean;
}

export interface PlayerStats {
  handsPlayed: number;
  handsWon: number;
  biggestPot: number;
  totalWinnings: number;
  vpipHands: number; // voluntarily put $ in pre-flop
}

export type Street = 'preflop' | 'flop' | 'turn' | 'river' | 'showdown';

export type ActionType = 'fold' | 'check' | 'call' | 'raise' | 'allin' | 'ante';

export interface HandAction {
  playerId: string;
  nickname: string;
  action: ActionType;
  amount: number;
  street: Street;
}

export interface HandResult {
  winnerId: string;
  winnerNickname: string;
  potWon: number;
  handName: string;
  handCards: CardCode[];
  allHands: { playerId: string; nickname: string; cards: CardCode[]; handName: string }[];
}

export type GamePhase =
  | 'waiting'      // not enough players
  | 'starting'     // countdown before hand
  | 'preflop'
  | 'flop'
  | 'turn'
  | 'river'
  | 'showdown'
  | 'result';      // showing result before next hand

export interface GameState {
  phase: GamePhase;
  mode: GameMode;
  communityCards: CardCode[];
  pot: number;
  sidePots: { amount: number; eligiblePlayerIds: string[] }[];
  currentPlayerIndex: number; // index into activePlayers
  dealerIndex: number;
  cucuruchoIndex: number;       // for cucurucho mode
  cucuruchoActive: boolean;     // true during a cucurucho hand
  cucuruchoPotSnapshot: number; // pot at flop reveal for cucurucho pay-in
  street: Street;
  minRaise: number;
  bigBlind: number;
  smallBlind: number;
  startingChips: number;
  actionHistory: HandAction[];
  lastResult: HandResult | null;
  handNumber: number;
  turnTimeLimit: number; // seconds
  turnStartedAt: number; // Date.now()
}

export interface RoomState {
  code: string;
  hostId: string;
  players: Player[];
  game: GameState | null;
  chatMessages: ChatMessage[];
  maxSeats: number;
}

export interface ChatMessage {
  id: string;
  playerId: string;
  nickname: string;
  text: string;
  timestamp: number;
}

// Socket event payloads
export interface JoinRoomPayload { code: string; nickname: string; asSpectator?: boolean }
export interface SitDownPayload { seatIndex: number }
export interface ActionPayload { type: ActionType; amount?: number }
export interface ChatPayload { text: string }
export interface CreateRoomPayload { nickname: string; bigBlind: number; startingChips: number; maxSeats: number; mode: GameMode; turnTimeLimit: number }
```

- [ ] Commit: `git add src/game/types.ts && git commit -m "feat: add shared game types"`

---

## Task 3: Deck + Hand Evaluator

**Files:** `src/game/deck.ts`, `src/game/hand-evaluator.ts`, `tests/game/deck.test.ts`, `tests/game/hand-evaluator.test.ts`

- [ ] Write failing tests `tests/game/deck.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { createDeck, shuffle, deal } from '@/game/deck';

describe('deck', () => {
  it('creates 52 unique cards', () => {
    const deck = createDeck();
    expect(deck).toHaveLength(52);
    expect(new Set(deck).size).toBe(52);
  });

  it('shuffle changes order (very likely)', () => {
    const a = createDeck();
    const b = shuffle([...a]);
    expect(a).not.toEqual(b);
  });

  it('deal removes cards from deck', () => {
    const deck = createDeck();
    const [cards, remaining] = deal(deck, 5);
    expect(cards).toHaveLength(5);
    expect(remaining).toHaveLength(47);
  });
});
```

- [ ] Write failing tests `tests/game/hand-evaluator.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { evaluateHand, compareHands, getHandName } from '@/game/hand-evaluator';

describe('hand-evaluator', () => {
  it('identifies royal flush', () => {
    const hand = evaluateHand(['As','Ks','Qs','Js','Ts']);
    expect(hand.name).toBe('Royal Flush');
  });

  it('identifies two pair', () => {
    const hand = evaluateHand(['As','Ah','Ks','Kh','2c','3d','4s']);
    expect(hand.name).toBe('Two Pair');
  });

  it('royal flush beats two pair', () => {
    const rf = evaluateHand(['As','Ks','Qs','Js','Ts']);
    const tp = evaluateHand(['As','Ah','Ks','Kh','2c']);
    const winners = compareHands([rf, tp]);
    expect(winners[0]).toBe(rf);
  });

  it('getHandName returns display string', () => {
    expect(getHandName('Royal Flush')).toBe('Royal Flush');
    expect(getHandName('Two Pair')).toBe('Dois Pares');
  });
});
```

- [ ] Run tests (expect fail): `npx vitest run tests/game/ 2>&1 | tail -5`

- [ ] Create `src/game/deck.ts`:

```ts
import type { CardCode, Rank, Suit } from './types';

const RANKS: Rank[] = ['2','3','4','5','6','7','8','9','T','J','Q','K','A'];
const SUITS: Suit[] = ['c','d','h','s'];

export function createDeck(): CardCode[] {
  const cards: CardCode[] = [];
  for (const rank of RANKS) {
    for (const suit of SUITS) {
      cards.push(`${rank}${suit}`);
    }
  }
  return cards;
}

export function shuffle(deck: CardCode[]): CardCode[] {
  const d = [...deck];
  for (let i = d.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [d[i], d[j]] = [d[j], d[i]];
  }
  return d;
}

export function deal(deck: CardCode[], count: number): [CardCode[], CardCode[]] {
  return [deck.slice(0, count), deck.slice(count)];
}

export function rankToDisplay(rank: string): string {
  return rank === 'T' ? '10' : rank;
}

export function suitToSymbol(suit: string): string {
  return { c: '♣', d: '♦', h: '♥', s: '♠' }[suit] ?? suit;
}

export function parseCard(code: CardCode): { rank: string; suit: string; display: string; symbol: string } {
  const rank = code.slice(0, -1);
  const suit = code.slice(-1);
  return { rank, suit, display: rankToDisplay(rank), symbol: suitToSymbol(suit) };
}
```

- [ ] Create `src/game/hand-evaluator.ts`:

```ts
import { Hand } from 'pokersolver';
import type { CardCode } from './types';

export interface EvaluatedHand {
  hand: ReturnType<typeof Hand.solve>;
  name: string;
  cards: CardCode[];
}

const PT_HAND_NAMES: Record<string, string> = {
  'Royal Flush': 'Royal Flush',
  'Straight Flush': 'Straight Flush',
  'Four of a Kind': 'Quadra',
  'Full House': 'Full House',
  'Flush': 'Flush',
  'Straight': 'Sequência',
  'Three of a Kind': 'Trinca',
  'Two Pair': 'Dois Pares',
  'Pair': 'Par',
  'High Card': 'Carta Alta',
};

export function evaluateHand(cards: CardCode[]): EvaluatedHand {
  const hand = Hand.solve(cards);
  return { hand, name: hand.name, cards };
}

export function compareHands(hands: EvaluatedHand[]): EvaluatedHand[] {
  const solved = hands.map(h => h.hand);
  const winnersSolved = Hand.winners(solved);
  return hands.filter(h => winnersSolved.includes(h.hand));
}

export function getHandName(englishName: string): string {
  return PT_HAND_NAMES[englishName] ?? englishName;
}
```

- [ ] Run tests (expect pass): `npx vitest run tests/game/ 2>&1 | tail -10`

- [ ] Commit: `git add -A && git commit -m "feat: deck and hand evaluator with tests"`

---

## Task 4: Texas Hold'em Engine

**Files:** `src/game/holdem.ts`, `tests/game/holdem.test.ts`

- [ ] Write failing tests `tests/game/holdem.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { createInitialGameState, getActivePlayers, getNextPlayerIndex, canCheck, getCallAmount } from '@/game/holdem';
import type { Player, GameState } from '@/game/types';

function makePlayer(id: string, chips: number, seatIndex: number): Player {
  return {
    id, nickname: id, seatIndex, chips, status: 'active',
    bet: 0, totalBet: 0, holeCards: [], isDealer: false, isSB: false, isBB: false,
    isConnected: true,
    stats: { handsPlayed: 0, handsWon: 0, biggestPot: 0, totalWinnings: 0, vpipHands: 0 },
  };
}

describe('holdem engine', () => {
  it('createInitialGameState sets correct blinds', () => {
    const state = createInitialGameState({ bigBlind: 20, startingChips: 1000, maxSeats: 9, mode: 'holdem', turnTimeLimit: 30 });
    expect(state.bigBlind).toBe(20);
    expect(state.smallBlind).toBe(10);
  });

  it('getActivePlayers excludes folded/sitting-out', () => {
    const players: Player[] = [
      { ...makePlayer('a', 100, 0), status: 'active' },
      { ...makePlayer('b', 100, 1), status: 'folded' },
      { ...makePlayer('c', 100, 2), status: 'active' },
    ];
    expect(getActivePlayers(players)).toHaveLength(2);
  });

  it('canCheck is true when no one has bet', () => {
    const p = makePlayer('a', 100, 0);
    expect(canCheck(p, 0)).toBe(true);
  });

  it('getCallAmount returns difference between current bet and player bet', () => {
    const p = { ...makePlayer('a', 100, 0), bet: 10 };
    expect(getCallAmount(p, 30)).toBe(20);
  });
});
```

- [ ] Run to confirm fail: `npx vitest run tests/game/holdem.test.ts 2>&1 | tail -5`

- [ ] Create `src/game/holdem.ts`:

```ts
import type { Player, GameState, Street, ActionType, HandAction, PlayerStatus, GameMode } from './types';
import { createDeck, shuffle, deal } from './deck';
import { evaluateHand, compareHands, getHandName } from './hand-evaluator';

interface GameConfig {
  bigBlind: number;
  startingChips: number;
  maxSeats: number;
  mode: GameMode;
  turnTimeLimit: number;
}

export function createInitialGameState(config: GameConfig): GameState {
  return {
    phase: 'waiting',
    mode: config.mode,
    communityCards: [],
    pot: 0,
    sidePots: [],
    currentPlayerIndex: 0,
    dealerIndex: 0,
    cucuruchoIndex: config.maxSeats - 1,
    cucuruchoActive: false,
    cucurchoPotSnapshot: 0,
    cucuruchoPotSnapshot: 0,
    street: 'preflop',
    minRaise: config.bigBlind * 2,
    bigBlind: config.bigBlind,
    smallBlind: config.bigBlind / 2,
    startingChips: config.startingChips,
    actionHistory: [],
    lastResult: null,
    handNumber: 0,
    turnTimeLimit: config.turnTimeLimit,
    turnStartedAt: 0,
  };
}

export function getSeatedPlayers(players: Player[]): Player[] {
  return players.filter(p => p.seatIndex !== null && p.status !== 'spectating' && p.isConnected);
}

export function getActivePlayers(players: Player[]): Player[] {
  return players.filter(p => p.status === 'active' || p.status === 'allin');
}

export function getPlayersStillIn(players: Player[]): Player[] {
  return players.filter(p => p.status === 'active' || p.status === 'allin');
}

export function canCheck(player: Player, currentBet: number): boolean {
  return player.bet >= currentBet;
}

export function getCallAmount(player: Player, currentBet: number): number {
  return Math.min(currentBet - player.bet, player.chips);
}

export function isAllInSituation(players: Player[]): boolean {
  const inPlay = getPlayersStillIn(players);
  return inPlay.filter(p => p.status === 'active').length <= 1;
}

export function dealHoleCards(players: Player[], deck: import('./types').CardCode[]): { players: Player[]; deck: import('./types').CardCode[] } {
  let remaining = deck;
  const updated = players.map(p => {
    if (p.seatIndex !== null && p.status === 'active') {
      const [cards, rest] = deal(remaining, 2);
      remaining = rest;
      return { ...p, holeCards: cards };
    }
    return p;
  });
  return { players: updated, deck: remaining };
}

export function dealCommunityCards(count: number, community: import('./types').CardCode[], deck: import('./types').CardCode[]): { community: import('./types').CardCode[]; deck: import('./types').CardCode[] } {
  const [cards, remaining] = deal(deck, count);
  return { community: [...community, ...cards], deck: remaining };
}

export function collectBetsIntoPot(players: Player[], pot: number): { players: Player[]; pot: number } {
  const collected = players.reduce((sum, p) => sum + p.bet, 0);
  const reset = players.map(p => ({ ...p, bet: 0 }));
  return { players: reset, pot: pot + collected };
}

export function postBlinds(players: Player[], sbIndex: number, bbIndex: number, sb: number, bb: number): Player[] {
  return players.map((p, i) => {
    if (i === sbIndex) {
      const amount = Math.min(sb, p.chips);
      return { ...p, chips: p.chips - amount, bet: amount, totalBet: amount, isSB: true, status: amount >= p.chips ? 'allin' : p.status };
    }
    if (i === bbIndex) {
      const amount = Math.min(bb, p.chips);
      return { ...p, chips: p.chips - amount, bet: amount, totalBet: amount, isBB: true, status: amount >= p.chips ? 'allin' : p.status };
    }
    return p;
  });
}

export function determineWinners(players: Player[], communityCards: import('./types').CardCode[]): { winnerId: string; winnerNickname: string; potWon: number; handName: string; handCards: import('./types').CardCode[]; allHands: { playerId: string; nickname: string; cards: import('./types').CardCode[]; handName: string }[] } {
  const contestants = getPlayersStillIn(players);
  const evaluated = contestants.map(p => ({
    player: p,
    evaluated: evaluateHand([...p.holeCards, ...communityCards]),
  }));

  const allHands = evaluated.map(e => ({
    playerId: e.player.id,
    nickname: e.player.nickname,
    cards: e.player.holeCards,
    handName: getHandName(e.evaluated.name),
  }));

  const winners = compareHands(evaluated.map(e => e.evaluated));
  const winnerEntry = evaluated.find(e => winners.includes(e.evaluated))!;

  return {
    winnerId: winnerEntry.player.id,
    winnerNickname: winnerEntry.player.nickname,
    potWon: 0, // filled by caller
    handName: getHandName(winnerEntry.evaluated.name),
    handCards: winnerEntry.player.holeCards,
    allHands,
  };
}

export function getNextActiveIndex(players: Player[], currentIndex: number): number {
  const len = players.length;
  for (let i = 1; i <= len; i++) {
    const idx = (currentIndex + i) % len;
    if (players[idx].status === 'active') return idx;
  }
  return -1;
}

export function getNextPlayerIndex(players: Player[], fromIndex: number): number {
  return getNextActiveIndex(players, fromIndex);
}

export function isBettingComplete(players: Player[], currentBet: number, lastAggressorIndex: number, currentPlayerIndex: number): boolean {
  const active = players.filter(p => p.status === 'active');
  if (active.length === 0) return true;
  const allCalled = active.every(p => p.bet === currentBet || p.chips === 0);
  return allCalled && currentPlayerIndex === lastAggressorIndex;
}
```

- [ ] Run tests: `npx vitest run tests/game/ 2>&1 | tail -10`

- [ ] Commit: `git add -A && git commit -m "feat: Texas Hold'em engine with tests"`

---

## Task 5: Cucurucho Mode

**Files:** `src/game/cucurucho.ts`

- [ ] Create `src/game/cucurucho.ts`:

```ts
import type { GameState, Player } from './types';

/** Returns true if this hand should be a Cucurucho hand */
export function isCucuruchoHand(state: GameState): boolean {
  if (state.mode !== 'cucurucho') return false;
  return state.dealerIndex === state.cucuruchoIndex;
}

/** Advance cucurucho button (moves one seat each hand, wrapping) */
export function advanceCucuruchoButton(state: GameState, totalSeats: number): GameState {
  return {
    ...state,
    cucuruchoIndex: (state.cucuruchoIndex + 1) % totalSeats,
  };
}

/** Ante amount for cucurucho hand = 10x big blind */
export function cucuruchoAnteAmount(bigBlind: number): number {
  return bigBlind * 10;
}

/** Apply antes to all seated players */
export function applyCucuruchoAntes(players: Player[], anteAmount: number): { players: Player[]; totalAnte: number } {
  let totalAnte = 0;
  const updated = players.map(p => {
    if (p.seatIndex === null || p.status === 'spectating') return p;
    const amount = Math.min(anteAmount, p.chips);
    totalAnte += amount;
    return {
      ...p,
      chips: p.chips - amount,
      bet: p.bet + amount,
      totalBet: p.totalBet + amount,
      status: p.chips - amount <= 0 ? ('allin' as const) : p.status,
    };
  });
  return { players: updated, totalAnte };
}

/** After flop in cucurucho: player must pay current pot to continue */
export function getCucuruchoPayInAmount(potSnapshot: number): number {
  return potSnapshot;
}
```

- [ ] Commit: `git add src/game/cucurucho.ts && git commit -m "feat: Cucurucho game mode logic"`

---

## Task 6: Server — Room Manager + Game Session

**Files:** `src/server/room-manager.ts`, `src/server/game-session.ts`

- [ ] Create `src/server/room-manager.ts`:

```ts
import type { RoomState, Player, GameState } from '@/game/types';
import { GameSession } from './game-session';

const rooms = new Map<string, GameSession>();

export function generateCode(): string {
  return Math.random().toString(36).substring(2, 8).toUpperCase();
}

export function createRoom(hostId: string, nickname: string, config: {
  bigBlind: number; startingChips: number; maxSeats: number;
  mode: import('@/game/types').GameMode; turnTimeLimit: number;
}): GameSession {
  let code = generateCode();
  while (rooms.has(code)) code = generateCode();
  const session = new GameSession(code, hostId, config);
  session.addPlayer(hostId, nickname, false);
  rooms.set(code, session);
  return session;
}

export function getRoom(code: string): GameSession | undefined {
  return rooms.get(code);
}

export function deleteRoom(code: string): void {
  rooms.delete(code);
}

export function getRoomByPlayerId(playerId: string): GameSession | undefined {
  for (const session of rooms.values()) {
    if (session.hasPlayer(playerId)) return session;
  }
  return undefined;
}
```

- [ ] Create `src/server/game-session.ts`:

```ts
import { EventEmitter } from 'events';
import type { RoomState, Player, GameState, GameMode, HandResult, ActionType, ChatMessage } from '@/game/types';
import { createInitialGameState, getSeatedPlayers, postBlinds, dealHoleCards, dealCommunityCards, collectBetsIntoPot, determineWinners, getNextActiveIndex, getCallAmount } from '@/game/holdem';
import { createDeck, shuffle } from '@/game/deck';
import { isCucuruchoHand, applyCucuruchoAntes, cucuruchoAnteAmount, advanceCucuruchoButton, getCucuruchoPayInAmount } from '@/game/cucurucho';

export class GameSession extends EventEmitter {
  code: string;
  hostId: string;
  players: Player[] = [];
  game: GameState;
  chatMessages: ChatMessage[] = [];
  maxSeats: number;
  private deck: string[] = [];
  private currentBet = 0;
  private lastAggressorIndex = 0;
  private turnTimer: ReturnType<typeof setTimeout> | null = null;

  constructor(code: string, hostId: string, config: {
    bigBlind: number; startingChips: number; maxSeats: number;
    mode: GameMode; turnTimeLimit: number;
  }) {
    super();
    this.code = code;
    this.hostId = hostId;
    this.maxSeats = config.maxSeats;
    this.game = createInitialGameState(config);
  }

  getRoomState(): RoomState {
    return {
      code: this.code,
      hostId: this.hostId,
      players: this.players.map(p => ({ ...p, holeCards: [] })), // never send hole cards to all
      game: this.game,
      chatMessages: this.chatMessages,
      maxSeats: this.maxSeats,
    };
  }

  getPlayerCards(playerId: string): string[] {
    return this.players.find(p => p.id === playerId)?.holeCards ?? [];
  }

  hasPlayer(playerId: string): boolean {
    return this.players.some(p => p.id === playerId);
  }

  addPlayer(id: string, nickname: string, asSpectator: boolean): Player {
    const existing = this.players.find(p => p.id === id);
    if (existing) return existing;
    const player: Player = {
      id, nickname,
      seatIndex: asSpectator ? null : null,
      chips: this.game.startingChips,
      status: asSpectator ? 'spectating' : 'waiting',
      bet: 0, totalBet: 0, holeCards: [],
      isDealer: false, isSB: false, isBB: false,
      isConnected: true,
      stats: { handsPlayed: 0, handsWon: 0, biggestPot: 0, totalWinnings: 0, vpipHands: 0 },
    };
    this.players.push(player);
    return player;
  }

  sitDown(playerId: string, seatIndex: number): boolean {
    const player = this.players.find(p => p.id === playerId);
    if (!player) return false;
    const taken = this.players.some(p => p.seatIndex === seatIndex);
    if (taken || seatIndex >= this.maxSeats) return false;
    player.seatIndex = seatIndex;
    player.status = 'waiting';
    return true;
  }

  leaveSeat(playerId: string): void {
    const player = this.players.find(p => p.id === playerId);
    if (player) { player.seatIndex = null; player.status = 'spectating'; }
  }

  removePlayer(playerId: string): void {
    if (this.game.phase !== 'waiting' && this.game.phase !== 'result') {
      const player = this.players.find(p => p.id === playerId);
      if (player) { player.isConnected = false; player.status = 'folded'; }
    } else {
      this.players = this.players.filter(p => p.id !== playerId);
    }
  }

  addChatMessage(playerId: string, text: string): ChatMessage {
    const player = this.players.find(p => p.id === playerId);
    const msg: ChatMessage = {
      id: Date.now().toString(),
      playerId,
      nickname: player?.nickname ?? 'Anon',
      text: text.slice(0, 200),
      timestamp: Date.now(),
    };
    this.chatMessages = [...this.chatMessages.slice(-99), msg];
    return msg;
  }

  canStart(): boolean {
    const seated = getSeatedPlayers(this.players);
    return seated.length >= 2 && this.game.phase === 'waiting';
  }

  startHand(): void {
    if (!this.canStart()) return;
    const seated = getSeatedPlayers(this.players);
    this.game = {
      ...this.game,
      phase: 'preflop',
      handNumber: this.game.handNumber + 1,
      communityCards: [],
      pot: 0,
      sidePots: [],
      actionHistory: [],
      lastResult: null,
    };

    // Reset player hand state
    this.players = this.players.map(p => ({
      ...p,
      bet: 0, totalBet: 0, holeCards: [],
      isDealer: false, isSB: false, isBB: false,
      status: p.seatIndex !== null && p.isConnected && p.chips > 0 ? 'active' : p.status,
    }));

    const seatedNow = getSeatedPlayers(this.players);
    const dealerIdx = this.game.dealerIndex % seatedNow.length;
    const sbIdx = (dealerIdx + 1) % seatedNow.length;
    const bbIdx = (dealerIdx + 2) % seatedNow.length;
    const utg = (bbIdx + 1) % seatedNow.length;

    seatedNow[dealerIdx].isDealer = true;

    // Cucurucho check
    const cucuruchoThisHand = isCucuruchoHand(this.game);
    if (cucuruchoThisHand) {
      const anteAmount = cucuruchoAnteAmount(this.game.bigBlind);
      const { players, totalAnte } = applyCucuruchoAntes(seatedNow, anteAmount);
      players.forEach(p => { const orig = this.players.find(o => o.id === p.id); if (orig) Object.assign(orig, p); });
      this.game.pot += totalAnte;
      this.game.cucuruchoActive = true;
    } else {
      this.game.cucuruchoActive = false;
    }

    // Post blinds
    const withBlinds = postBlinds(seatedNow, sbIdx, bbIdx, this.game.smallBlind, this.game.bigBlind);
    withBlinds.forEach(p => { const orig = this.players.find(o => o.id === p.id); if (orig) Object.assign(orig, p); });

    this.currentBet = this.game.bigBlind;
    this.lastAggressorIndex = bbIdx;

    // Deal hole cards
    this.deck = shuffle(createDeck());
    const { players: withCards, deck } = dealHoleCards(this.players, this.deck);
    this.players = withCards;
    this.deck = deck;

    this.game.currentPlayerIndex = utg;
    this.game.dealerIndex = dealerIdx;
    this.game.street = 'preflop';
    this.game.turnStartedAt = Date.now();

    this.emit('state-updated');
    this.startTurnTimer();
  }

  handleAction(playerId: string, type: ActionType, amount?: number): void {
    const seatedPlayers = getSeatedPlayers(this.players);
    const currentPlayer = seatedPlayers[this.game.currentPlayerIndex];
    if (!currentPlayer || currentPlayer.id !== playerId) return;
    if (currentPlayer.status !== 'active') return;

    this.clearTurnTimer();
    const action = { playerId, nickname: currentPlayer.nickname, action: type, amount: amount ?? 0, street: this.game.street };

    if (type === 'fold') {
      currentPlayer.status = 'folded';
    } else if (type === 'check') {
      // no-op
    } else if (type === 'call') {
      const callAmt = getCallAmount(currentPlayer, this.currentBet);
      currentPlayer.chips -= callAmt;
      currentPlayer.bet += callAmt;
      currentPlayer.totalBet += callAmt;
      if (currentPlayer.chips === 0) currentPlayer.status = 'allin';
      action.amount = callAmt;
      currentPlayer.stats.vpipHands++;
    } else if (type === 'raise') {
      const raiseTotal = amount ?? this.currentBet * 2;
      const diff = raiseTotal - currentPlayer.bet;
      currentPlayer.chips -= diff;
      currentPlayer.bet = raiseTotal;
      currentPlayer.totalBet += diff;
      if (currentPlayer.chips === 0) currentPlayer.status = 'allin';
      this.currentBet = raiseTotal;
      this.game.minRaise = raiseTotal + (raiseTotal - this.currentBet);
      this.lastAggressorIndex = this.game.currentPlayerIndex;
      action.amount = raiseTotal;
      currentPlayer.stats.vpipHands++;
    } else if (type === 'allin') {
      const allInAmt = currentPlayer.chips;
      currentPlayer.bet += allInAmt;
      currentPlayer.totalBet += allInAmt;
      currentPlayer.chips = 0;
      currentPlayer.status = 'allin';
      if (currentPlayer.bet > this.currentBet) {
        this.currentBet = currentPlayer.bet;
        this.lastAggressorIndex = this.game.currentPlayerIndex;
      }
      action.amount = allInAmt;
    }

    this.game.actionHistory.push(action);

    const stillActive = seatedPlayers.filter(p => p.status === 'active');
    if (stillActive.length <= 1 && seatedPlayers.filter(p => p.status === 'allin').length === 0) {
      // everyone folded except one
      this.resolveSingleWinner();
      return;
    }

    const nextIdx = this.advanceToNextStreetOrPlayer(seatedPlayers);
    this.game.currentPlayerIndex = nextIdx;
    this.game.turnStartedAt = Date.now();
    this.emit('state-updated');
    this.startTurnTimer();
  }

  private advanceToNextStreetOrPlayer(seatedPlayers: Player[]): number {
    const nextIdx = getNextActiveIndex(seatedPlayers, this.game.currentPlayerIndex);
    const allDone = seatedPlayers.filter(p => p.status === 'active').every(p => p.bet === this.currentBet);
    const loopedBack = nextIdx === this.lastAggressorIndex || nextIdx === -1;

    if (allDone && loopedBack) {
      this.advanceStreet(seatedPlayers);
      return this.game.currentPlayerIndex;
    }
    return nextIdx === -1 ? 0 : nextIdx;
  }

  private advanceStreet(seatedPlayers: Player[]): void {
    const { players, pot } = collectBetsIntoPot(seatedPlayers, this.game.pot);
    players.forEach(p => { const orig = this.players.find(o => o.id === p.id); if (orig) Object.assign(orig, p); });
    this.game.pot = pot;
    this.currentBet = 0;
    this.game.minRaise = this.game.bigBlind;

    const streets: Record<string, string> = { preflop: 'flop', flop: 'turn', turn: 'river', river: 'showdown' };
    const next = streets[this.game.street];

    if (next === 'flop') {
      const { community, deck } = dealCommunityCards(3, [], this.deck);
      this.game.communityCards = community; this.deck = deck;
      this.game.street = 'flop'; this.game.phase = 'flop';

      if (this.game.cucuruchoActive) {
        this.game.cucuruchoPotSnapshot = this.game.pot;
        this.game.phase = 'flop';
        // Emit special cucurucho-pay-in event - players must call pot or fold
        this.emit('cucurucho-pay-in');
        return;
      }
    } else if (next === 'turn') {
      const { community, deck } = dealCommunityCards(1, this.game.communityCards, this.deck);
      this.game.communityCards = community; this.deck = deck;
      this.game.street = 'turn'; this.game.phase = 'turn';
    } else if (next === 'river') {
      const { community, deck } = dealCommunityCards(1, this.game.communityCards, this.deck);
      this.game.communityCards = community; this.deck = deck;
      this.game.street = 'river'; this.game.phase = 'river';
    } else if (next === 'showdown') {
      this.resolveShowdown();
      return;
    }

    const sbIdx = (this.game.dealerIndex + 1) % seatedPlayers.length;
    this.game.currentPlayerIndex = getNextActiveIndex(seatedPlayers, this.game.dealerIndex);
    this.lastAggressorIndex = this.game.currentPlayerIndex;
  }

  handleCucuruchoPayIn(playerId: string, payIn: boolean): void {
    const player = this.players.find(p => p.id === playerId);
    if (!player || player.status !== 'active') return;
    if (payIn) {
      const amount = Math.min(getCucuruchoPayInAmount(this.game.cucuruchoPotSnapshot), player.chips);
      player.chips -= amount;
      player.bet += amount;
      player.totalBet += amount;
      this.game.pot += amount;
      if (player.chips === 0) player.status = 'allin';
    } else {
      player.status = 'folded';
    }

    const seated = getSeatedPlayers(this.players);
    const allActed = seated.every(p => p.status !== 'active' || p.bet >= this.game.cucuruchoPotSnapshot);
    if (allActed) {
      // Deal turn + river without betting, then showdown
      const { community: withTurn, deck: d1 } = dealCommunityCards(1, this.game.communityCards, this.deck);
      const { community: withRiver, deck: d2 } = dealCommunityCards(1, withTurn, d1);
      this.game.communityCards = withRiver;
      this.deck = d2;
      this.resolveShowdown();
    } else {
      this.emit('state-updated');
    }
  }

  private resolveSingleWinner(): void {
    const seated = getSeatedPlayers(this.players);
    const winner = seated.find(p => p.status === 'active') ?? seated.find(p => p.status === 'allin');
    if (!winner) return;
    winner.chips += this.game.pot;
    winner.stats.handsWon++;
    winner.stats.totalWinnings += this.game.pot;
    if (this.game.pot > winner.stats.biggestPot) winner.stats.biggestPot = this.game.pot;

    this.game.lastResult = {
      winnerId: winner.id,
      winnerNickname: winner.nickname,
      potWon: this.game.pot,
      handName: '',
      handCards: [],
      allHands: [],
    };
    this.game.phase = 'result';
    this.clearTurnTimer();
    this.emit('state-updated');
    setTimeout(() => this.prepareNextHand(), 4000);
  }

  private resolveShowdown(): void {
    const { players, pot } = collectBetsIntoPot(this.players, this.game.pot);
    this.players = players;
    this.game.pot = pot;

    const result = determineWinners(this.players, this.game.communityCards);
    const winner = this.players.find(p => p.id === result.winnerId)!;
    winner.chips += this.game.pot;
    winner.stats.handsWon++;
    winner.stats.totalWinnings += this.game.pot;
    if (this.game.pot > winner.stats.biggestPot) winner.stats.biggestPot = this.game.pot;

    // Update all players stats
    const seated = getSeatedPlayers(this.players);
    seated.forEach(p => { p.stats.handsPlayed++; });

    this.game.lastResult = { ...result, potWon: this.game.pot };
    this.game.phase = 'result';
    this.clearTurnTimer();
    this.emit('state-updated');
    setTimeout(() => this.prepareNextHand(), 5000);
  }

  private prepareNextHand(): void {
    // Advance dealer
    const seated = getSeatedPlayers(this.players);
    this.game.dealerIndex = (this.game.dealerIndex + 1) % Math.max(seated.length, 1);
    if (this.game.mode === 'cucurucho') {
      const updated = advanceCucuruchoButton(this.game, this.maxSeats);
      this.game.cucuruchoIndex = updated.cucuruchoIndex;
    }
    // Remove busted players
    this.players = this.players.map(p => {
      if (p.chips <= 0 && p.seatIndex !== null) {
        return { ...p, chips: this.game.startingChips, status: 'waiting' }; // rebuy auto
      }
      return p;
    });
    this.game.phase = 'waiting';
    this.emit('state-updated');
    if (this.canStart()) setTimeout(() => this.startHand(), 2000);
  }

  private startTurnTimer(): void {
    if (this.game.turnTimeLimit <= 0) return;
    this.turnTimer = setTimeout(() => {
      const seated = getSeatedPlayers(this.players);
      const current = seated[this.game.currentPlayerIndex];
      if (current) this.handleAction(current.id, 'fold');
    }, this.game.turnTimeLimit * 1000);
  }

  private clearTurnTimer(): void {
    if (this.turnTimer) { clearTimeout(this.turnTimer); this.turnTimer = null; }
  }
}
```

- [ ] Commit: `git add -A && git commit -m "feat: server room manager and game session"`

---

## Task 7: Socket.io Handlers

**Files:** `src/server/socket-handlers.ts`

- [ ] Create `src/server/socket-handlers.ts`:

```ts
import type { Server, Socket } from 'socket.io';
import { createRoom, getRoom, getRoomByPlayerId } from './room-manager';
import type { CreateRoomPayload, JoinRoomPayload, SitDownPayload, ActionPayload, ChatPayload } from '@/game/types';

export function setupSocketHandlers(io: Server): void {
  io.on('connection', (socket: Socket) => {
    const playerId = socket.id;

    function emitRoomState(code: string): void {
      const session = getRoom(code);
      if (!session) return;
      // emit base state to all (without hole cards)
      io.to(code).emit('room-state', session.getRoomState());
      // emit private hole cards to each player
      session.players.forEach(p => {
        const cards = session.getPlayerCards(p.id);
        io.to(p.id).emit('your-cards', cards);
      });
    }

    socket.on('create-room', (payload: CreateRoomPayload) => {
      const session = createRoom(playerId, payload.nickname, {
        bigBlind: payload.bigBlind ?? 20,
        startingChips: payload.startingChips ?? 1000,
        maxSeats: payload.maxSeats ?? 9,
        mode: payload.mode ?? 'holdem',
        turnTimeLimit: payload.turnTimeLimit ?? 30,
      });
      session.on('state-updated', () => emitRoomState(session.code));
      session.on('cucurucho-pay-in', () => {
        io.to(session.code).emit('cucurucho-pay-in', { potAmount: session.game.cucuruchoPotSnapshot });
        emitRoomState(session.code);
      });
      socket.join(session.code);
      socket.join(playerId);
      emitRoomState(session.code);
    });

    socket.on('join-room', (payload: JoinRoomPayload) => {
      const session = getRoom(payload.code);
      if (!session) { socket.emit('error', { message: 'Sala não encontrada' }); return; }
      session.addPlayer(playerId, payload.nickname, payload.asSpectator ?? false);
      session.on('state-updated', () => emitRoomState(session.code));
      session.on('cucurucho-pay-in', () => {
        io.to(session.code).emit('cucurucho-pay-in', { potAmount: session.game.cucuruchoPotSnapshot });
        emitRoomState(session.code);
      });
      socket.join(payload.code);
      socket.join(playerId);
      emitRoomState(payload.code);
    });

    socket.on('sit-down', (payload: SitDownPayload) => {
      const session = getRoomByPlayerId(playerId);
      if (!session) return;
      session.sitDown(playerId, payload.seatIndex);
      emitRoomState(session.code);
    });

    socket.on('leave-seat', () => {
      const session = getRoomByPlayerId(playerId);
      if (!session) return;
      session.leaveSeat(playerId);
      emitRoomState(session.code);
    });

    socket.on('start-game', () => {
      const session = getRoomByPlayerId(playerId);
      if (!session || session.hostId !== playerId) return;
      session.startHand();
    });

    socket.on('action', (payload: ActionPayload) => {
      const session = getRoomByPlayerId(playerId);
      if (!session) return;
      session.handleAction(playerId, payload.type, payload.amount);
    });

    socket.on('cucurucho-decision', (payload: { payIn: boolean }) => {
      const session = getRoomByPlayerId(playerId);
      if (!session) return;
      session.handleCucuruchoPayIn(playerId, payload.payIn);
      emitRoomState(session.code);
    });

    socket.on('chat-message', (payload: ChatPayload) => {
      const session = getRoomByPlayerId(playerId);
      if (!session) return;
      const msg = session.addChatMessage(playerId, payload.text);
      io.to(session.code).emit('chat-message', msg);
    });

    socket.on('disconnect', () => {
      const session = getRoomByPlayerId(playerId);
      if (!session) return;
      session.removePlayer(playerId);
      emitRoomState(session.code);
    });
  });
}
```

- [ ] Commit: `git add src/server/socket-handlers.ts && git commit -m "feat: Socket.io event handlers"`

---

## Task 8: Client Socket + Zustand Store

**Files:** `src/lib/socket.ts`, `src/lib/store.ts`, `src/lib/hooks/useGame.ts`, `src/lib/hooks/useTimer.ts`

- [ ] Create `src/lib/socket.ts`:

```ts
import { io, Socket } from 'socket.io-client';

let socket: Socket | null = null;

export function getSocket(): Socket {
  if (!socket) {
    socket = io(typeof window !== 'undefined' ? window.location.origin : 'http://localhost:3000', {
      transports: ['websocket'],
    });
  }
  return socket;
}

export function disconnectSocket(): void {
  socket?.disconnect();
  socket = null;
}
```

- [ ] Create `src/lib/store.ts`:

```ts
import { create } from 'zustand';
import type { RoomState, CardCode, ChatMessage } from '@/game/types';

interface GameStore {
  roomState: RoomState | null;
  myCards: CardCode[];
  nickname: string;
  roomCode: string;
  cucuruchoPayInActive: boolean;
  cucuruchoPayInAmount: number;
  setRoomState: (state: RoomState) => void;
  setMyCards: (cards: CardCode[]) => void;
  setNickname: (n: string) => void;
  setRoomCode: (c: string) => void;
  setCucuruchoPayIn: (active: boolean, amount: number) => void;
  addChatMessage: (msg: ChatMessage) => void;
}

export const useGameStore = create<GameStore>((set) => ({
  roomState: null,
  myCards: [],
  nickname: '',
  roomCode: '',
  cucuruchoPayInActive: false,
  cucuruchoPayInAmount: 0,
  setRoomState: (roomState) => set({ roomState }),
  setMyCards: (myCards) => set({ myCards }),
  setNickname: (nickname) => set({ nickname }),
  setRoomCode: (roomCode) => set({ roomCode }),
  setCucuruchoPayIn: (cucuruchoPayInActive, cucuruchoPayInAmount) =>
    set({ cucuruchoPayInActive, cucuruchoPayInAmount }),
  addChatMessage: (msg) =>
    set((s) => ({
      roomState: s.roomState
        ? { ...s.roomState, chatMessages: [...(s.roomState.chatMessages ?? []).slice(-99), msg] }
        : null,
    })),
}));
```

- [ ] Create `src/lib/hooks/useGame.ts`:

```ts
'use client';
import { useEffect, useCallback } from 'react';
import { getSocket } from '@/lib/socket';
import { useGameStore } from '@/lib/store';
import type { ActionType, CreateRoomPayload, GameMode } from '@/game/types';

export function useGame() {
  const store = useGameStore();
  const socket = getSocket();

  useEffect(() => {
    socket.on('room-state', store.setRoomState);
    socket.on('your-cards', store.setMyCards);
    socket.on('cucurucho-pay-in', ({ potAmount }: { potAmount: number }) => {
      store.setCucuruchoPayIn(true, potAmount);
    });
    socket.on('chat-message', store.addChatMessage);
    return () => {
      socket.off('room-state');
      socket.off('your-cards');
      socket.off('cucurucho-pay-in');
      socket.off('chat-message');
    };
  }, []);

  const createRoom = useCallback((payload: CreateRoomPayload) => {
    store.setNickname(payload.nickname);
    socket.emit('create-room', payload);
  }, []);

  const joinRoom = useCallback((code: string, nickname: string, asSpectator = false) => {
    store.setNickname(nickname);
    store.setRoomCode(code);
    socket.emit('join-room', { code, nickname, asSpectator });
  }, []);

  const sitDown = useCallback((seatIndex: number) => {
    socket.emit('sit-down', { seatIndex });
  }, []);

  const leaveSeat = useCallback(() => socket.emit('leave-seat'), []);

  const startGame = useCallback(() => socket.emit('start-game'), []);

  const sendAction = useCallback((type: ActionType, amount?: number) => {
    socket.emit('action', { type, amount });
  }, []);

  const sendCucuruchoDecision = useCallback((payIn: boolean) => {
    socket.emit('cucurucho-decision', { payIn });
    store.setCucuruchoPayIn(false, 0);
  }, []);

  const sendChat = useCallback((text: string) => {
    socket.emit('chat-message', { text });
  }, []);

  return {
    ...store,
    createRoom, joinRoom, sitDown, leaveSeat, startGame,
    sendAction, sendCucuruchoDecision, sendChat,
    myId: socket.id,
  };
}
```

- [ ] Create `src/lib/hooks/useTimer.ts`:

```ts
'use client';
import { useEffect, useState } from 'react';

export function useTimer(startedAt: number, limitSeconds: number): number {
  const [remaining, setRemaining] = useState(limitSeconds);

  useEffect(() => {
    if (!startedAt || !limitSeconds) return;
    const tick = () => {
      const elapsed = (Date.now() - startedAt) / 1000;
      setRemaining(Math.max(0, limitSeconds - elapsed));
    };
    tick();
    const id = setInterval(tick, 250);
    return () => clearInterval(id);
  }, [startedAt, limitSeconds]);

  return remaining;
}
```

- [ ] Create `src/lib/sounds.ts`:

```ts
import { Howl } from 'howler';

const soundUrls: Record<string, string> = {
  fold:  'https://assets.mixkit.co/active_storage/sfx/2000/2000-preview.mp3',
  check: 'https://assets.mixkit.co/active_storage/sfx/2568/2568-preview.mp3',
  call:  'https://assets.mixkit.co/active_storage/sfx/2357/2357-preview.mp3',
  raise: 'https://assets.mixkit.co/active_storage/sfx/2072/2072-preview.mp3',
  deal:  'https://assets.mixkit.co/active_storage/sfx/2077/2077-preview.mp3',
  win:   'https://assets.mixkit.co/active_storage/sfx/1434/1434-preview.mp3',
  chip:  'https://assets.mixkit.co/active_storage/sfx/2012/2012-preview.mp3',
};

const cache: Record<string, Howl> = {};

export function playSound(name: keyof typeof soundUrls): void {
  if (typeof window === 'undefined') return;
  if (!cache[name]) {
    cache[name] = new Howl({ src: [soundUrls[name]], volume: 0.5 });
  }
  cache[name].play();
}
```

- [ ] Commit: `git add -A && git commit -m "feat: client socket singleton, Zustand store, and hooks"`

---

## Task 9: Card Component

**Files:** `src/components/cards/Card.tsx`, `src/components/cards/CardBack.tsx`

- [ ] Create `src/components/cards/Card.tsx`:

```tsx
'use client';
import { motion } from 'framer-motion';
import { parseCard } from '@/game/deck';

interface Props {
  code: string;
  small?: boolean;
  highlight?: boolean;
}

const suitColor: Record<string, string> = {
  h: '#e53e3e', d: '#e53e3e', s: '#1a202c', c: '#1a202c',
};

export function Card({ code, small, highlight }: Props) {
  const { display, symbol, suit } = parseCard(code);
  const size = small ? 'w-10 h-14 text-sm' : 'w-14 h-20 text-base';

  return (
    <motion.div
      initial={{ rotateY: 90, scale: 0.8 }}
      animate={{ rotateY: 0, scale: 1 }}
      transition={{ duration: 0.3 }}
      className={`${size} rounded-lg flex flex-col items-start justify-between p-1 select-none font-bold shadow-lg
        ${highlight ? 'ring-2 ring-yellow-400' : ''}
        bg-gradient-to-br from-white to-gray-50 border border-gray-200`}
      style={{ color: suitColor[suit] }}
    >
      <div className="leading-none">{display}</div>
      <div className="self-center text-xl leading-none">{symbol}</div>
      <div className="self-end rotate-180 leading-none">{display}</div>
    </motion.div>
  );
}
```

- [ ] Create `src/components/cards/CardBack.tsx`:

```tsx
'use client';
import { motion } from 'framer-motion';

interface Props { small?: boolean }

export function CardBack({ small }: Props) {
  const size = small ? 'w-10 h-14' : 'w-14 h-20';
  return (
    <motion.div
      initial={{ scale: 0 }}
      animate={{ scale: 1 }}
      className={`${size} rounded-lg shadow-lg border border-gray-700
        bg-gradient-to-br from-blue-900 to-blue-700`}
      style={{
        backgroundImage: `repeating-linear-gradient(
          45deg, rgba(255,255,255,0.05) 0px, rgba(255,255,255,0.05) 2px,
          transparent 2px, transparent 8px
        )`,
      }}
    />
  );
}
```

- [ ] Commit: `git add -A && git commit -m "feat: Card and CardBack components"`

---

## Task 10: Poker Table Layout

**Files:** `src/components/table/TableFelt.tsx`, `src/components/table/CommunityCards.tsx`, `src/components/table/Pot.tsx`, `src/components/table/PlayerSeat.tsx`

- [ ] Create `src/components/table/TableFelt.tsx`:

```tsx
export function TableFelt({ children }: { children: React.ReactNode }) {
  return (
    <div className="relative w-full" style={{ paddingBottom: '50%' }}>
      <div className="absolute inset-0 rounded-[50%] border-8 border-[#3d2b1f] shadow-2xl"
        style={{ background: 'radial-gradient(ellipse at center, #2d6a4f 0%, #1a472a 70%, #0f3320 100%)' }}>
        <div className="absolute inset-4 rounded-[50%] border border-[#2d6a4f] opacity-30" />
        {children}
      </div>
    </div>
  );
}
```

- [ ] Create `src/components/table/CommunityCards.tsx`:

```tsx
'use client';
import { Card } from '@/components/cards/Card';
import { CardBack } from '@/components/cards/CardBack';

interface Props {
  cards: string[];
  phase: string;
}

const TOTAL = 5;

export function CommunityCards({ cards, phase }: Props) {
  return (
    <div className="flex gap-2 justify-center items-center">
      {Array.from({ length: TOTAL }).map((_, i) => {
        if (i < cards.length) return <Card key={i} code={cards[i]} />;
        return null;
      })}
    </div>
  );
}
```

- [ ] Create `src/components/table/Pot.tsx`:

```tsx
interface Props { pot: number }

export function Pot({ pot }: Props) {
  if (pot <= 0) return null;
  return (
    <div className="flex flex-col items-center gap-1">
      <div className="flex gap-1">
        {[...Array(Math.min(Math.ceil(pot / 100), 6))].map((_, i) => (
          <div key={i} className="w-5 h-5 rounded-full border-2 border-yellow-500
            bg-gradient-to-b from-yellow-300 to-yellow-600 shadow-sm" />
        ))}
      </div>
      <span className="text-yellow-300 text-sm font-bold drop-shadow">Pot: {pot.toLocaleString()}</span>
    </div>
  );
}
```

- [ ] Create `src/components/table/PlayerSeat.tsx` (show player info around the table):

```tsx
'use client';
import { Card } from '@/components/cards/Card';
import { CardBack } from '@/components/cards/CardBack';
import type { Player } from '@/game/types';

interface Props {
  player: Player;
  myCards: string[];
  isMe: boolean;
  isCurrentTurn: boolean;
}

export function PlayerSeat({ player, myCards, isMe, isCurrentTurn }: Props) {
  const cards = isMe ? myCards : player.holeCards;

  return (
    <div className={`flex flex-col items-center gap-1 p-2 rounded-xl transition-all
      ${isCurrentTurn ? 'ring-2 ring-yellow-400 bg-yellow-400/10' : ''}
      ${!player.isConnected ? 'opacity-40' : ''}`}
    >
      {/* Cards */}
      <div className="flex gap-1">
        {player.status === 'folded' ? (
          <span className="text-gray-500 text-xs">FOLD</span>
        ) : cards.length > 0 ? (
          cards.map((c, i) => <Card key={i} code={c} small />)
        ) : (player.status === 'active' || player.status === 'allin') ? (
          <><CardBack small /><CardBack small /></>
        ) : null}
      </div>

      {/* Avatar + Name */}
      <div className={`w-10 h-10 rounded-full flex items-center justify-center font-bold text-sm
        ${isMe ? 'bg-blue-600' : 'bg-gray-700'} border-2
        ${player.isDealer ? 'border-yellow-400' : isCurrentTurn ? 'border-green-400' : 'border-gray-600'}`}>
        {player.nickname[0].toUpperCase()}
      </div>

      <div className="text-center">
        <div className="text-white text-xs font-semibold truncate max-w-[80px]">{player.nickname}</div>
        <div className="text-yellow-300 text-xs">{player.chips.toLocaleString()}</div>
        {player.bet > 0 && (
          <div className="text-green-400 text-xs">Bet: {player.bet}</div>
        )}
      </div>

      {/* Badges */}
      <div className="flex gap-1">
        {player.isDealer && <span className="text-[10px] bg-white text-black rounded-full px-1 font-bold">D</span>}
        {player.isSB && <span className="text-[10px] bg-blue-500 text-white rounded-full px-1 font-bold">SB</span>}
        {player.isBB && <span className="text-[10px] bg-red-500 text-white rounded-full px-1 font-bold">BB</span>}
        {player.status === 'allin' && <span className="text-[10px] bg-orange-500 text-white rounded px-1 font-bold">ALL IN</span>}
      </div>
    </div>
  );
}
```

- [ ] Commit: `git add -A && git commit -m "feat: table layout components"`

---

## Task 11: Betting Controls + Timer

**Files:** `src/components/table/BettingControls.tsx`, `src/components/table/ActionTimer.tsx`

- [ ] Create `src/components/table/ActionTimer.tsx`:

```tsx
'use client';
import { useTimer } from '@/lib/hooks/useTimer';

interface Props { startedAt: number; limitSeconds: number }

export function ActionTimer({ startedAt, limitSeconds }: Props) {
  const remaining = useTimer(startedAt, limitSeconds);
  const pct = (remaining / limitSeconds) * 100;
  const color = remaining < 10 ? 'bg-red-500' : remaining < 20 ? 'bg-yellow-500' : 'bg-green-500';

  return (
    <div className="w-full h-1.5 bg-gray-700 rounded-full overflow-hidden">
      <div className={`h-full ${color} transition-all duration-250`} style={{ width: `${pct}%` }} />
    </div>
  );
}
```

- [ ] Create `src/components/table/BettingControls.tsx`:

```tsx
'use client';
import { useState } from 'react';
import type { GameState, Player } from '@/game/types';
import { getCallAmount, canCheck } from '@/game/holdem';
import { ActionTimer } from './ActionTimer';

interface Props {
  game: GameState;
  me: Player;
  onAction: (type: string, amount?: number) => void;
}

export function BettingControls({ game, me, onAction }: Props) {
  const [raiseAmount, setRaiseAmount] = useState(game.minRaise);

  const currentBet = Math.max(...[0]); // simplified, passed from parent
  const callAmt = getCallAmount(me, game.minRaise); // TODO: pass currentBet properly
  const canCheckNow = canCheck(me, 0); // simplified

  return (
    <div className="flex flex-col gap-2 p-4 bg-gray-900/90 rounded-xl border border-gray-700">
      <ActionTimer startedAt={game.turnStartedAt} limitSeconds={game.turnTimeLimit} />

      <div className="flex gap-2">
        <button
          onClick={() => onAction('fold')}
          className="flex-1 py-2 bg-red-700 hover:bg-red-600 text-white font-bold rounded-lg transition"
        >Fold</button>

        <button
          onClick={() => onAction(canCheckNow ? 'check' : 'call')}
          className="flex-1 py-2 bg-blue-700 hover:bg-blue-600 text-white font-bold rounded-lg transition"
        >
          {canCheckNow ? 'Check' : `Call ${callAmt}`}
        </button>

        <button
          onClick={() => onAction('allin')}
          className="flex-1 py-2 bg-orange-600 hover:bg-orange-500 text-white font-bold rounded-lg transition"
        >All In</button>
      </div>

      <div className="flex gap-2 items-center">
        <input
          type="range"
          min={game.minRaise}
          max={me.chips + me.bet}
          value={raiseAmount}
          onChange={e => setRaiseAmount(Number(e.target.value))}
          className="flex-1 accent-yellow-400"
        />
        <button
          onClick={() => onAction('raise', raiseAmount)}
          className="px-4 py-2 bg-yellow-600 hover:bg-yellow-500 text-white font-bold rounded-lg transition whitespace-nowrap"
        >
          Raise {raiseAmount}
        </button>
      </div>
    </div>
  );
}
```

- [ ] Commit: `git add -A && git commit -m "feat: betting controls and action timer"`

---

## Task 12: Chat + Hand History + Winner Overlay

**Files:** `src/components/table/Chat.tsx`, `src/components/table/HandHistory.tsx`, `src/components/table/WinnerOverlay.tsx`

- [ ] Create `src/components/table/Chat.tsx`:

```tsx
'use client';
import { useState, useRef, useEffect } from 'react';
import type { ChatMessage } from '@/game/types';

interface Props {
  messages: ChatMessage[];
  onSend: (text: string) => void;
  nickname: string;
}

export function Chat({ messages, onSend, nickname }: Props) {
  const [text, setText] = useState('');
  const bottomRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    bottomRef.current?.scrollIntoView({ behavior: 'smooth' });
  }, [messages]);

  const send = () => {
    const t = text.trim();
    if (!t) return;
    onSend(t);
    setText('');
  };

  return (
    <div className="flex flex-col h-48 bg-gray-900 rounded-xl border border-gray-700">
      <div className="flex-1 overflow-y-auto p-2 space-y-1">
        {messages.map(m => (
          <div key={m.id} className="text-xs">
            <span className={`font-bold ${m.nickname === nickname ? 'text-blue-400' : 'text-green-400'}`}>
              {m.nickname}:
            </span>{' '}
            <span className="text-gray-300">{m.text}</span>
          </div>
        ))}
        <div ref={bottomRef} />
      </div>
      <div className="flex border-t border-gray-700">
        <input
          value={text}
          onChange={e => setText(e.target.value)}
          onKeyDown={e => e.key === 'Enter' && send()}
          placeholder="Mensagem..."
          className="flex-1 bg-transparent px-2 py-1 text-xs text-white outline-none"
          maxLength={200}
        />
        <button onClick={send} className="px-3 text-blue-400 hover:text-blue-300 text-xs font-bold">
          Enviar
        </button>
      </div>
    </div>
  );
}
```

- [ ] Create `src/components/table/HandHistory.tsx`:

```tsx
import type { HandResult } from '@/game/types';
import { Card } from '@/components/cards/Card';

interface Props { result: HandResult | null }

export function HandHistory({ result }: Props) {
  if (!result) return null;
  return (
    <div className="bg-gray-800 rounded-xl p-3 border border-gray-700 text-sm">
      <div className="text-yellow-400 font-bold mb-2">Última Mão</div>
      <div className="text-white mb-1">
        🏆 <strong>{result.winnerNickname}</strong> ganhou{' '}
        <span className="text-green-400">{result.potWon.toLocaleString()}</span> fichas
        {result.handName && ` com ${result.handName}`}
      </div>
      {result.handCards.length > 0 && (
        <div className="flex gap-1 mt-1">
          {result.handCards.map((c, i) => <Card key={i} code={c} small />)}
        </div>
      )}
      {result.allHands.length > 1 && (
        <div className="mt-2 space-y-1">
          {result.allHands.map(h => (
            <div key={h.playerId} className="text-gray-300 text-xs flex items-center gap-2">
              <span>{h.nickname}:</span>
              <div className="flex gap-1">
                {h.cards.map((c, i) => <Card key={i} code={c} small />)}
              </div>
              <span className="text-gray-400">({h.handName})</span>
            </div>
          ))}
        </div>
      )}
    </div>
  );
}
```

- [ ] Create `src/components/table/WinnerOverlay.tsx`:

```tsx
'use client';
import { motion, AnimatePresence } from 'framer-motion';
import type { HandResult } from '@/game/types';

interface Props { result: HandResult | null; show: boolean }

export function WinnerOverlay({ result, show }: Props) {
  return (
    <AnimatePresence>
      {show && result && (
        <motion.div
          initial={{ opacity: 0, scale: 0.5 }}
          animate={{ opacity: 1, scale: 1 }}
          exit={{ opacity: 0, scale: 0.5 }}
          className="absolute inset-0 flex items-center justify-center z-50 pointer-events-none"
        >
          <div className="bg-black/80 rounded-2xl px-8 py-6 text-center border border-yellow-400 shadow-2xl">
            <div className="text-4xl mb-2">🏆</div>
            <div className="text-yellow-400 text-2xl font-bold">{result.winnerNickname}</div>
            <div className="text-white text-lg">{result.handName}</div>
            <div className="text-green-400 text-xl font-bold mt-1">+{result.potWon.toLocaleString()}</div>
          </div>
        </motion.div>
      )}
    </AnimatePresence>
  );
}
```

- [ ] Commit: `git add -A && git commit -m "feat: chat, hand history, winner overlay"`

---

## Task 13: Main Poker Table Page

**Files:** `src/components/table/PokerTable.tsx`, `src/app/room/[code]/page.tsx`

- [ ] Create `src/components/table/PokerTable.tsx`:

```tsx
'use client';
import { useEffect } from 'react';
import { useGame } from '@/lib/hooks/useGame';
import { TableFelt } from './TableFelt';
import { PlayerSeat } from './PlayerSeat';
import { CommunityCards } from './CommunityCards';
import { Pot } from './Pot';
import { BettingControls } from './BettingControls';
import { Chat } from './Chat';
import { HandHistory } from './HandHistory';
import { WinnerOverlay } from './WinnerOverlay';
import { playSound } from '@/lib/sounds';

// Seat positions around oval (percentage-based, clockwise from bottom-left)
const SEAT_POSITIONS = [
  { bottom: '2%', left: '20%' },
  { bottom: '2%', left: '50%', transform: 'translateX(-50%)' },
  { bottom: '2%', right: '20%' },
  { top: '40%', right: '2%' },
  { top: '5%', right: '20%' },
  { top: '5%', left: '50%', transform: 'translateX(-50%)' },
  { top: '5%', left: '20%' },
  { top: '40%', left: '2%' },
  { bottom: '25%', left: '2%' },
];

interface Props { roomCode: string }

export function PokerTable({ roomCode }: Props) {
  const game = useGame();
  const { roomState, myCards, nickname, sendAction, sendChat, sitDown, startGame, sendCucuruchoDecision, cucuruchoPayInActive, cucuruchoPayInAmount } = game;

  if (!roomState) {
    return <div className="text-white text-center mt-20">Conectando...</div>;
  }

  const { players, game: gs, chatMessages, hostId } = roomState;
  const myId = game.myId;
  const me = players.find(p => p.id === myId);
  const seated = players.filter(p => p.seatIndex !== null);
  const seatedSorted = [...seated].sort((a, b) => (a.seatIndex ?? 0) - (b.seatIndex ?? 0));
  const isMyTurn = gs && me && seatedSorted[gs.currentPlayerIndex]?.id === myId && (gs.phase !== 'waiting' && gs.phase !== 'result');
  const isHost = myId === hostId;
  const canStartGame = isHost && gs?.phase === 'waiting' && seated.filter(p => p.isConnected).length >= 2;

  return (
    <div className="min-h-screen bg-gray-950 flex flex-col lg:flex-row gap-4 p-4">
      {/* Table */}
      <div className="flex-1 flex flex-col gap-4">
        {/* Game mode + code */}
        <div className="flex items-center justify-between">
          <div className="text-gray-400 text-sm">
            Sala: <span className="text-white font-mono font-bold">{roomCode}</span>
            {gs && <span className="ml-3 text-yellow-400">{gs.mode === 'cucurucho' ? '🌀 Cucurucho' : '♠ Hold\'em'}</span>}
          </div>
          {gs && gs.phase !== 'waiting' && (
            <div className="text-gray-400 text-sm">Mão #{gs.handNumber}</div>
          )}
        </div>

        {/* Oval Table */}
        <div className="relative w-full max-w-3xl mx-auto">
          <TableFelt>
            {/* Community cards center */}
            <div className="absolute inset-0 flex flex-col items-center justify-center gap-2">
              {gs && <Pot pot={gs.pot} />}
              {gs && <CommunityCards cards={gs.communityCards} phase={gs.phase} />}
              {gs?.cucuruchoActive && (
                <div className="text-yellow-400 text-xs font-bold animate-pulse">🌀 CUCURUCHO!</div>
              )}
            </div>

            {/* Player seats around table */}
            {SEAT_POSITIONS.map((pos, seatIdx) => {
              const player = seatedSorted.find(p => p.seatIndex === seatIdx);
              if (!player) {
                return (
                  <button
                    key={seatIdx}
                    onClick={() => !me?.seatIndex && sitDown(seatIdx)}
                    className="absolute text-gray-500 hover:text-gray-300 text-xs border border-dashed border-gray-700 rounded-lg px-2 py-1"
                    style={pos as React.CSSProperties}
                  >
                    + Sentar
                  </button>
                );
              }
              return (
                <div key={seatIdx} className="absolute" style={pos as React.CSSProperties}>
                  <PlayerSeat
                    player={player}
                    myCards={myCards}
                    isMe={player.id === myId}
                    isCurrentTurn={gs ? seatedSorted[gs.currentPlayerIndex]?.id === player.id : false}
                  />
                </div>
              );
            })}

            {/* Winner overlay */}
            {gs && <WinnerOverlay result={gs.lastResult} show={gs.phase === 'result'} />}
          </TableFelt>
        </div>

        {/* Controls */}
        <div className="max-w-3xl mx-auto w-full space-y-3">
          {canStartGame && (
            <button onClick={startGame}
              className="w-full py-3 bg-green-600 hover:bg-green-500 text-white font-bold rounded-xl text-lg transition">
              Iniciar Jogo
            </button>
          )}

          {cucuruchoPayInActive && me && (
            <div className="bg-yellow-900/50 border border-yellow-500 rounded-xl p-4 text-center">
              <div className="text-yellow-300 font-bold mb-2">🌀 Cucurucho! Pagar {cucuruchoPayInAmount} para continuar?</div>
              <div className="flex gap-3 justify-center">
                <button onClick={() => sendCucuruchoDecision(true)}
                  className="px-6 py-2 bg-green-600 hover:bg-green-500 text-white font-bold rounded-lg">
                  Pagar ({cucuruchoPayInAmount})
                </button>
                <button onClick={() => sendCucuruchoDecision(false)}
                  className="px-6 py-2 bg-red-700 hover:bg-red-600 text-white font-bold rounded-lg">
                  Fold
                </button>
              </div>
            </div>
          )}

          {isMyTurn && me && gs && !cucuruchoPayInActive && (
            <BettingControls game={gs} me={me} onAction={(type, amount) => { sendAction(type as any, amount); playSound(type as any); }} />
          )}
        </div>
      </div>

      {/* Sidebar */}
      <div className="w-full lg:w-80 flex flex-col gap-4">
        <Chat messages={chatMessages ?? []} onSend={sendChat} nickname={nickname} />
        {gs && <HandHistory result={gs.lastResult} />}
        {/* Player stats */}
        {me && (
          <div className="bg-gray-900 rounded-xl p-3 border border-gray-700">
            <div className="text-yellow-400 font-bold mb-2 text-sm">Meus Stats</div>
            <div className="space-y-1 text-xs text-gray-300">
              <div>Mãos jogadas: {me.stats.handsPlayed}</div>
              <div>Vitórias: {me.stats.handsWon}</div>
              <div>Maior pote: {me.stats.biggestPot.toLocaleString()}</div>
              <div>Total ganho: {me.stats.totalWinnings.toLocaleString()}</div>
              <div>VPIP: {me.stats.handsPlayed > 0
                ? `${Math.round((me.stats.vpipHands / me.stats.handsPlayed) * 100)}%`
                : '—'}</div>
            </div>
          </div>
        )}
      </div>
    </div>
  );
}
```

- [ ] Create `src/app/room/[code]/page.tsx`:

```tsx
'use client';
import { useEffect } from 'react';
import { useParams } from 'next/navigation';
import { PokerTable } from '@/components/table/PokerTable';
import { useGame } from '@/lib/hooks/useGame';
import { useGameStore } from '@/lib/store';

export default function RoomPage() {
  const { code } = useParams<{ code: string }>();
  const { joinRoom, roomState } = useGame();
  const { nickname } = useGameStore();

  useEffect(() => {
    if (code && nickname) {
      joinRoom(code, nickname);
    }
  }, [code, nickname]);

  if (!nickname) {
    return (
      <div className="min-h-screen bg-gray-950 flex items-center justify-center">
        <div className="text-white text-center">
          <p>Volte para a página inicial para entrar na sala.</p>
          <a href="/" className="text-blue-400 underline mt-2 block">Página inicial</a>
        </div>
      </div>
    );
  }

  return <PokerTable roomCode={code} />;
}
```

- [ ] Commit: `git add -A && git commit -m "feat: main poker table page and orchestrator"`

---

## Task 14: Home Screen

**Files:** `src/components/home/HomeScreen.tsx`, `src/app/page.tsx`, `src/app/layout.tsx`, `src/app/globals.css`

- [ ] Create `src/app/globals.css`:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --bg: #0a0e1a;
  --felt: #1a472a;
}

body {
  background-color: var(--bg);
  font-family: 'Inter', sans-serif;
}

* { box-sizing: border-box; }
```

- [ ] Create `src/app/layout.tsx`:

```tsx
import type { Metadata } from 'next';
import './globals.css';

export const metadata: Metadata = {
  title: 'Poker Online',
  description: 'Jogue poker com amigos',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="pt-BR">
      <body className="bg-gray-950 text-white min-h-screen">{children}</body>
    </html>
  );
}
```

- [ ] Create `src/components/home/HomeScreen.tsx`:

```tsx
'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { useGame } from '@/lib/hooks/useGame';
import type { GameMode } from '@/game/types';

export function HomeScreen() {
  const router = useRouter();
  const { createRoom, joinRoom } = useGame();
  const [tab, setTab] = useState<'create' | 'join'>('create');
  const [nickname, setNickname] = useState('');
  const [joinCode, setJoinCode] = useState('');
  const [bigBlind, setBigBlind] = useState(20);
  const [startingChips, setStartingChips] = useState(1000);
  const [maxSeats, setMaxSeats] = useState(6);
  const [mode, setMode] = useState<GameMode>('holdem');
  const [turnTimeLimit, setTurnTimeLimit] = useState(30);
  const [asSpectator, setAsSpectator] = useState(false);
  const [roomCode, setRoomCode] = useState('');

  const { getSocket } = require('@/lib/socket'); // lazy import

  const handleCreate = () => {
    if (!nickname.trim()) return;
    import('@/lib/socket').then(({ getSocket }) => {
      const socket = getSocket();
      socket.once('room-state', (state: any) => {
        router.push(`/room/${state.code}`);
      });
    });
    createRoom({ nickname: nickname.trim(), bigBlind, startingChips, maxSeats, mode, turnTimeLimit });
  };

  const handleJoin = () => {
    if (!nickname.trim() || !joinCode.trim()) return;
    import('@/lib/socket').then(({ getSocket }) => {
      const socket = getSocket();
      socket.once('room-state', () => {
        router.push(`/room/${joinCode.toUpperCase().trim()}`);
      });
    });
    joinRoom(joinCode.toUpperCase().trim(), nickname.trim(), asSpectator);
  };

  return (
    <div className="min-h-screen flex flex-col items-center justify-center bg-gray-950 p-4">
      <div className="text-center mb-8">
        <h1 className="text-5xl font-black text-white mb-2">
          ♠ <span className="text-yellow-400">Poker</span> Online
        </h1>
        <p className="text-gray-400">Jogue com seus amigos</p>
      </div>

      <div className="w-full max-w-md bg-gray-900 rounded-2xl p-6 border border-gray-800 shadow-2xl">
        {/* Tabs */}
        <div className="flex rounded-xl bg-gray-800 p-1 mb-6">
          {(['create', 'join'] as const).map(t => (
            <button key={t} onClick={() => setTab(t)}
              className={`flex-1 py-2 rounded-lg text-sm font-bold transition
                ${tab === t ? 'bg-yellow-500 text-black' : 'text-gray-400 hover:text-white'}`}>
              {t === 'create' ? 'Criar Sala' : 'Entrar na Sala'}
            </button>
          ))}
        </div>

        <div className="space-y-4">
          <input value={nickname} onChange={e => setNickname(e.target.value)}
            placeholder="Seu apelido"
            className="w-full bg-gray-800 text-white px-4 py-3 rounded-xl border border-gray-700 outline-none focus:border-yellow-500 transition"
            maxLength={20} />

          {tab === 'create' ? (
            <>
              <div className="grid grid-cols-2 gap-3">
                <label className="block">
                  <span className="text-gray-400 text-xs">Big Blind</span>
                  <input type="number" value={bigBlind} onChange={e => setBigBlind(Number(e.target.value))}
                    className="w-full bg-gray-800 text-white px-3 py-2 rounded-lg border border-gray-700 outline-none mt-1" />
                </label>
                <label className="block">
                  <span className="text-gray-400 text-xs">Fichas iniciais</span>
                  <input type="number" value={startingChips} onChange={e => setStartingChips(Number(e.target.value))}
                    className="w-full bg-gray-800 text-white px-3 py-2 rounded-lg border border-gray-700 outline-none mt-1" />
                </label>
                <label className="block">
                  <span className="text-gray-400 text-xs">Assentos (máx)</span>
                  <select value={maxSeats} onChange={e => setMaxSeats(Number(e.target.value))}
                    className="w-full bg-gray-800 text-white px-3 py-2 rounded-lg border border-gray-700 outline-none mt-1">
                    {[2,3,4,5,6,7,8,9].map(n => <option key={n} value={n}>{n}</option>)}
                  </select>
                </label>
                <label className="block">
                  <span className="text-gray-400 text-xs">Tempo/turno (s)</span>
                  <input type="number" value={turnTimeLimit} onChange={e => setTurnTimeLimit(Number(e.target.value))}
                    className="w-full bg-gray-800 text-white px-3 py-2 rounded-lg border border-gray-700 outline-none mt-1" />
                </label>
              </div>

              <div>
                <span className="text-gray-400 text-xs block mb-2">Modo de jogo</span>
                <div className="flex gap-2">
                  {([['holdem', "♠ Hold'em"], ['cucurucho', '🌀 Cucurucho']] as [GameMode, string][]).map(([m, label]) => (
                    <button key={m} onClick={() => setMode(m)}
                      className={`flex-1 py-2 rounded-lg text-sm font-bold border transition
                        ${mode === m ? 'bg-yellow-500 text-black border-yellow-500' : 'bg-gray-800 text-gray-300 border-gray-700 hover:border-gray-500'}`}>
                      {label}
                    </button>
                  ))}
                </div>
              </div>

              <button onClick={handleCreate} disabled={!nickname.trim()}
                className="w-full py-3 bg-green-600 hover:bg-green-500 disabled:opacity-50 text-white font-bold rounded-xl text-lg transition">
                Criar Sala
              </button>
            </>
          ) : (
            <>
              <input value={joinCode} onChange={e => setJoinCode(e.target.value.toUpperCase())}
                placeholder="Código da sala (ex: AB12CD)"
                className="w-full bg-gray-800 text-white px-4 py-3 rounded-xl border border-gray-700 outline-none focus:border-yellow-500 transition font-mono"
                maxLength={6} />
              <label className="flex items-center gap-2 text-gray-400 text-sm cursor-pointer">
                <input type="checkbox" checked={asSpectator} onChange={e => setAsSpectator(e.target.checked)}
                  className="accent-yellow-400" />
                Entrar como espectador
              </label>
              <button onClick={handleJoin} disabled={!nickname.trim() || !joinCode.trim()}
                className="w-full py-3 bg-blue-600 hover:bg-blue-500 disabled:opacity-50 text-white font-bold rounded-xl text-lg transition">
                Entrar na Sala
              </button>
            </>
          )}
        </div>
      </div>
    </div>
  );
}
```

- [ ] Create `src/app/page.tsx`:

```tsx
import { HomeScreen } from '@/components/home/HomeScreen';
export default function Home() { return <HomeScreen />; }
```

- [ ] Commit: `git add -A && git commit -m "feat: home screen with create/join room"`

---

## Task 15: Final Wiring + Run

- [ ] Ensure `next.config.ts` does NOT conflict with custom server:

```ts
import type { NextConfig } from 'next';
const nextConfig: NextConfig = {};
export default nextConfig;
```

- [ ] Add `.gitignore` entries: `echo ".next\nnode_modules\n.superpowers\n" >> .gitignore`

- [ ] Run dev server: `npm run dev`

- [ ] Open `http://localhost:3000` — verify home screen loads.

- [ ] Open two browser tabs, create a room in one, join with the code in the other, sit down in both, start game — verify cards are dealt and betting works.

- [ ] Commit: `git add -A && git commit -m "feat: complete poker app wiring"`

---

## Task 16: Run All Tests

- [ ] Run: `npx vitest run`

- [ ] Expected output: all `tests/game/` tests pass (deck, hand-evaluator, holdem).

- [ ] If any fail: fix the failing test before moving on.

- [ ] Commit if any fixes: `git add -A && git commit -m "fix: test failures"`

---

## Self-Review Checklist

- [x] Spec coverage: Texas Hold'em ✓, Cucurucho ✓, room by code ✓, no-auth ✓, cash game ✓, chat ✓, spectators ✓, hand history ✓, stats ✓, sounds ✓, animations ✓
- [x] No placeholders: all tasks have real code
- [x] Type consistency: `CardCode`, `Player`, `GameState` used consistently throughout
- [x] `cucuruchoPayInAmount` spelling consistent (note: GameState has `cucuruchoPotSnapshot`, store has `cucuruchoPayInAmount` — both intentional with different roles)
- [x] `getSocket` imported from `@/lib/socket` consistently
